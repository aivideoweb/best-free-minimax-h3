# Sources and adaptation record

This VideoWeb AI directory adapts [Flaq AI’s MiniMax H3 cookbook](https://github.com/flaqai/awesome-minimax-h3-video-prompts) at commit `9fed21c196ffa5495b8f6d8de29cc77ba71eb66d`, retrieved 2026-09-22. The original 84 recipes, 24 category files, 12 reference visuals, templates and supporting guides retain their source attribution and MIT notice. They were not newly authored or generated for this adaptation.

Nine additional X case leads and frame-review notes came from the sibling [VideoWeb prompt project](https://github.com/aivideoweb/awesome-minimax-h3-prompts). All 15 post texts and video metadata, plus the XH3-014 prompt reply, were retrieved again via FxTwitter on 2026-09-22. Previous frame-review observations are attributed to that earlier review, not a new complete playback. No generation was performed for this directory.

New material: tool-first English and Chinese homepages; 13 tool profiles and short-shot practice prompts; VideoWeb navigation artwork; comparison and maintenance records. Tool use cases are editorial suggestions, not comparative output tests.

## Page evidence

The [tool manifest](../data/tools.json) records one status per entry. On 2026-09-22, 12 direct public HTML reads succeeded. AITryOn returned HTTP 403 to that client, but subsequently loaded in the actual browser. Expanding the output FAQ on UGC Maker, HeyDream, Flaq, BestImage and AITryOn confirmed five-second 480p clips; the other pages state those settings in visible text. All 13 now have page-level evidence. The VideoWeb form was also visually checked and [captured](../assets/videoweb-free-form.jpg). No CAPTCHA was submitted and no generation queue was exercised. HTTP failure does not imply the browser route is unavailable.

Per-tool records include evidence method, a short exact snippet, date, URL and an HTML hash where captured. The named FAQ panels were “What video settings are available?” (UGC Maker), “What is the video length and format?” (HeyDream), “What video format does this free tool provide?” (BestImage), and “What video length, resolution, and aspect ratios are available?” (Flaq and AITryOn).

A page claim is not a measured output result. Unknown duration, resolution, watermark, rights or speed remains unknown. The directory contains related company brands, not 13 independently benchmarked services. No evidence establishes distinct backends or relative quality.

## External media

X previews and MP4s remain remote and creator-owned; official examples remain with MiniMax. The MIT license does not grant rights to these external works or the H3 model weights. The gallery links complete author prompts rather than copying them. An uploaded video's resolution is not a free-tool specification.

## Render and link checks

On 2026-09-22, the local Markdown preview loaded all 15 remote X thumbnails. The HTTP client’s thumbnail HEAD requests returned 403, while browser image rendering succeeded. All 15 MP4 HEAD requests returned 200 and video/mp4. These checks establish current link response and preview loading, not full playback or model authenticity. The English homepage and a tool profile were visually inspected with a GitHub-like Markdown stylesheet; this was a local preview, not a GitHub production-render assertion.

The sibling VideoWeb source manifest was also verified through GitHub’s contents API: it matches the imported local source byte for byte at revision `19cae6029b258b670c2bf888a8b320bcb2eb951d`. Earlier frame-review links are pinned to the exact source revision and case anchor; their original check dates are retained separately from this directory’s new metadata check.

The [quality review record](./quality-review.md) summarizes three review rounds and the final direct source comparison. It records content/navigation checks, not a model-performance certification.
